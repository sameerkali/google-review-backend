MONGO_URI=mongodb://localhost:27017/expendifii
PORT=5000
# Required — the server refuses to start without this.
JWT_SECRET=change-me
ADMIN_USERNAME=admin
ADMIN_PASSWORD=adminPassword
NODE_ENV=development
# Optional, comma-separated allowlist (e.g. https://app.example.com,https://admin.example.com).
# Leave unset to allow all origins (current default behavior).
CORS_ORIGIN=